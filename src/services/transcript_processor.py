from typing import Iterable, List


class TranscriptProcessor:
    def process(self, transcript_stream: Iterable[str]) -> List[str]:
        # Placeholder processor
        return list(transcript_stream)
