## ▶️ Использование

### Запуск транскрибирования:
```bash
python cli.py start
Удаление всех моделей:

python cli.py clean
Удаление только результатов:

python cli.py clean-results
Проверка состояния моделей:

python cli.py check
Просмотр списка сохраненных результатов:

python cli.py list
Просмотр содержимого результата:

python cli.py show summary_20240101_120000.txt
📁 Где сохраняются результаты?

Все результаты сохраняются в папке results/:

Транскрипции: transcript_YYYYMMDD_HHMMSS.txt
Саммари: summary_YYYYMMDD_HHMMSS.txt
При каждом запуске создаются новые файлы с уникальными именами.


---

## 🎯 Где генерируется саммари?

**Саммари сохраняется в двух местах:**

1. **В консоль** - сразу после завершения встречи
2. **В файл** - в папке `results/` с именем `summary_YYYYMMDD_HHMMSS.txt`

### Пример структуры файлов после использования:

meeting_summarizer/ ├── results/ │ ├── transcript_20240101_143022.txt │ └── summary_20240101_143022.txt ├── models/ │ └── TinyLlama-1.1B-Chat-v1.0.Q4_K_M.gguf └── ...

### Пример содержимого summary файла:

Саммари встречи от 2024-01-01 14:30:22

На встрече обсуждались ключевые вопросы проекта. Участники согласовали сроки сдачи и распределили ответственность за реализацию различных компонентов. Следующая встреча запланирована на следующей неделе.